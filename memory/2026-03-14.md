# 2026-03-14

- 继续推进 `execution-system-spec-v1`，将当前 focus 从 `ES-F.F2` 推进到 `ES-F.F3`。
- 已在 `docs/execution-system-spec-v1.md` 冻结第一条 checker 的 exact contract：明确 hard-fail 字段、defer 项、失败输出格式与人类恢复路径。
- 已在 `tasks/execution-system-spec-v1-tasks.md` 新增 `ES-F.F3`，将下一刀明确为按 F2 contract 实现 ACTIVE field checker。
- 已更新 `roadmaps/execution-system-spec-v1-roadmap.md`，当前推荐阶段切到 `ES-F.F3`。
- 已改造 `scripts/check_active_consistency.py`：第一波硬校验改为只强制当前 focus roadmap activity 满足 `source_doc` / `roadmap_doc` / `tasks_doc` / `current_slice_id` / `next_slice_id` / `last_commit` 以及非空 `next_step` / `validation` / `blocked_by`；非 focus roadmap activity 不再被过早一刀切。
- 一致性检查通过：`python3 /Users/erictao/.openclaw/workspace/scripts/check_active_consistency.py` 返回 `CONSISTENCY_CHECK_OK`。
- 已继续收紧 `ES-F.F3`：为 workspace repo 自身的 `last_commit` 自引用漂移补上更直白的错误口径；当只改了 `ACTIVE.md` 时，checker 会提示这更可能是 ledger self-drift，而不是泛化的 repo 异常。
- 已统一 checker 的字段/列表报错 helper，减少 `missing field` / `missing list` 口径分叉；同时为 focus activity 的 `source_doc` / `roadmap_doc` / `tasks_doc` 增加目标文件存在性校验。
- 已把 focus 从 `ES-F.F3` 推进到 `ES-F.F4`，并新增 `scripts/test_check_active_consistency.py` 作为第一条 checker 的最小 smoke 覆盖；当前已覆盖 happy path、缺失 `source_doc`、以及坏 `roadmap_doc` 指针三类场景。
- 已继续把 focus 推到 `ES-F.F5`：开始冻结第二条 checker（migrated task slice schema checker）的 exact contract，目标是只检查已迁移 slices 的 `phase_id` / `rollback_strategy`，不误伤 legacy task sections。
- 已继续推进到 `ES-F.F7`：新增 `scripts/check_task_slice_schema.py`，实现第二条 checker 的第一版 hard-fail 逻辑；同时补齐 `tasks/one-publish-refactor-tasks.md` 中多处 migrated slices 的 `rollback_strategy`，并修正 checker 对 list-style 字段的解析。当前 `check_task_slice_schema.py` 已在 one-publish 样本上返回 `TASK_SLICE_SCHEMA_CHECK_OK`。
- 已推进到 `ES-F.F8`：新增 `scripts/test_check_task_slice_schema.py` 作为第二条 checker 的最小 smoke 覆盖，当前已覆盖 happy path、legacy `Task` heading 不误伤、以及 migrated `Slice` 缺 `rollback_strategy` 的 hard-fail。中途修正了一次测试样本构造，避免被真实文档中的重复 `rollback_strategy` 掩盖。
- 已推进到 `ES-F.F9`：新增统一入口 `scripts/run_execution_system_checks.py`，现在可以一条命令串行执行 active checker / active smoke / task-slice checker / task-slice smoke，并在失败时返回非零退出码。当前统一入口已返回 `EXECUTION_SYSTEM_CHECKS_OK`。
- 已推进 `ES-F.F10`：`scripts/complete_slice.sh prepare` 与 `scripts/accept_active_ledger_v2.py` 已复用统一 checker 入口。联调时确认 unified suite 本身通过；`accept_active_ledger_v2.py` 仍因独立的 `focus-first` gate 失败，这是因为当前 focus activity 故意保持 `autopilot: false`，属于预期策略结果，不是接线故障。
- 已推进 `ES-F.F11`：把操作入口规则落盘到 `docs/execution-system-spec-v1.md` 与 `HEARTBEAT.md`。现在 routine validation 默认走 `run_execution_system_checks.py`；只修 `ACTIVE.md` 时走 `check_active_consistency.py`；只迭代 migrated task schema 时走 `check_task_slice_schema.py`。heartbeat 的 active work check 也已改成优先跑统一 checker 套件。
- 已推进 `ES-F.F12`：把 Phase 6 的 completion gate / expansion gate 冻结进 `docs/execution-system-spec-v1.md`、`tasks/execution-system-spec-v1-tasks.md`、`roadmaps/execution-system-spec-v1-roadmap.md`。现在门槛已经明确：当前两条 checker + smoke + 统一入口 + workflow adoption + 日常入口规则齐备后，默认不应机械继续加第三条 hard-fail 规则，除非出现新的确定性缺口信号。
- 已推进 `ES-F.F13`：把当前 Phase 6 的收官结论写清楚。现在默认结论已经固定：当前两条 hard-fail checker wave 应视为 sufficiently landed，下一波优先转 advisory/workflow 改进，不再默认追加第三条 hard-fail checker；只有后续出现新的明确 deterministic gap 才重开。
- 已推进 `ES-F.F14`：把 post-Phase-6 的下一波候选排成明确顺序，并把 roadmap/tasks/ACTIVE 一起切到新的 advisory/workflow next-wave 入口。当前推荐顺序已固定为：1) oversized migration slice advisory，2) workflow-level failure summary / recovery hints，3) strategy-gate UX 改进；默认都先走 advisory/workflow，不直接升级为 hard-fail。
- 已推进 `ES-F.F15`：冻结 `oversized migration slice advisory` 的 exact contract。当前已明确：它只看 migrated slice semantics，只发 advisory warning，不 hard-fail；信号主要围绕“一个 slice 下累积了多类异质子目标，导致人难以回答这刀到底落了什么”，恢复路径是审视该 slice 是否需要拆分或收紧描述。
- 已推进 `ES-F.F16`：把 oversized-slice advisory 收敛成第一版 deterministic trigger。当前触发边界已固定为：migrated `#### Slice ...` heading + 有 `phase_id` + 多项 `scope` + 多项 `done_definition`，且不把已拆分的 `2A/2B` 风格 slice 默认打成 oversized；`one-publish` 的 `Slice P2` 作为首个 reference trigger sample。
- 已推进 `ES-F.F17`：新增 `scripts/check_oversized_migration_slices.py`，实现第一版 oversized migration slice advisory。首版已保持 advisory-only，并通过收紧为“至少两个结构信号同时命中”减少噪声；当前在 `one-publish` 样本上主要提示 `Slice C1/C2/C3/C4` 与 `Slice P1` 这类明显偏宽的 migrated slices。
- 已推进 `ES-F.F18`：新增 `scripts/test_check_oversized_migration_slices.py` 作为 oversized advisory 的最小 smoke 覆盖，当前已覆盖 broad migrated slice 触发 advisory、已拆分 `2A` 风格 slice 不告警、以及 legacy `Task` heading 不进入 advisory 三类场景。
- 已推进 `ES-F.F19`：把 oversized advisory 接进 `scripts/run_execution_system_checks.py` 的 workflow-level reporting。现在 unified runner 会额外打印 oversized advisory 输出，但仍保持 hard-fail suite 的退出语义不变；当前 `EXECUTION_SYSTEM_CHECKS_OK` 与 advisory 输出可同时出现。
- 已推进 `ES-F.F20`：把 oversized advisory reporting 从原始命中列表收口成更清楚的恢复型输出。当前输出已包含 `warning_count`、逐条结构原因和统一 `recovery_hint`，同时保留每个命中 slice 的具体原因，不会只剩空泛摘要。
- 已推进 `ES-F.F21`：冻结 `workflow-level failure summary / recovery hints` 的 exact contract。当前口径已明确：summary 层只做 operator-facing 总结，不替代底层输出；需要区分 hard-fail / advisory / strategy-gate 三类结果，并给出对应 recovery mapping。
- 已推进 `ES-F.F22`：把 workflow-level failure summary 收敛成第一版可编码输出格式。当前已明确第一版只追加 summary footer，不重排原始 checker/advisory 输出；footer 至少包含 hard-fail status、first failing command、advisory hits、以及按域划分的 recovery hints。
- 已推进 `ES-F.F23`：在 `scripts/run_execution_system_checks.py` 中落地第一版 workflow summary footer。当前 unified runner 在保留全部 raw output 的前提下，额外输出 `EXECUTION_SYSTEM_SUMMARY`，包含 hard_fail_status、first_failing_command、advisory_hits、advisory_command 与 recovery_hint。
- 已推进 `ES-F.F24`：新增 `scripts/test_run_execution_system_checks.py`，为 workflow summary footer 补最小 smoke 覆盖。当前已覆盖 hard-fail failed 路径与 pass+advisory 路径，并把 footer 逻辑收成可单测的 `summary_footer()`，避免继续依赖脆弱的进程 stub。
- 已推进 `ES-F.F25`：把 slice completion state machine 的 `closed_out` 正式推进到工件语义。`create_slice_closeout.py` 现在会写入 `slice_state: closed_out`，`check_slice_closeout.py` 会拒绝非 `closed_out` 状态；同时新增 `scripts/test_slice_closeout_state.py` 作为最小 smoke，验证 `closed_out` 会落盘而错误状态会被 verifier 拒绝。
- 已推进 `ES-F.F26`：把 `validated` 也推进成显式工件字段。现在 closeout artifact 同时包含 `validation_state: validated` 与 `slice_state: closed_out`，`check_slice_closeout.py` 会同时校验这两个字段；对应 smoke 也已扩展，能拒绝错误的 `validation_state` 与错误的 `slice_state`。
- 已推进 `ES-F.F27/F28`：把 ACTIVE 第二轮瘦身边界冻结并实际落地到 `execution-system-spec-v1` 这张 roadmap activity card。当前 `last_artifact` / `last_decision` 已从长流水压缩为恢复必要指针，保留了 checker suite、advisory layer、closeout state semantics、one-publish anchors 等运行时恢复关键信息，同时去掉了大量阶段性历史里程碑枚举。
- 已推进 `ES-F.F29`：把 acceptance / strategy-gate 语义对齐到最终规范。现在 `validate_focus_first.py` 会在当前 focus 故意不是 auto-runnable 时返回 `FOCUS_VALIDATION_POLICY_GATE`，`accept_active_ledger_v2.py` 则返回 `ACTIVE_LEDGER_V2_ACCEPTANCE_OK_WITH_POLICY_GATES`，不再把这种预期策略状态误报成 acceptance failure。
- 已推进 `ES-F.F30`：把当前这轮对齐最终规范 v1 的补齐结果收成 audit / completion 结论。当前文档已明确：`closed_out` 与 `validated` 已进入 closeout artifact 语义、policy gate 不再被误报成 acceptance failure、ACTIVE 已完成第二轮瘦身；剩余差距更偏增量优化项，而不是基础模型缺口。
- 已推进 `ES-F.F31`：冻结未来重开 execution-system 主线的条件。当前规则已明确：只有出现新的结构缺口、重复恢复歧义、重复误导性 checker/acceptance 结果，或真实项目迁移暴露六层模型无法表达的情况时，才应重开；单纯“还能再 polish 一点”不足以继续线性扩 execution-system 主线。
- 已进一步把 execution-system activity 的默认语义收口成维护态：roadmap / ACTIVE / spec 现在都明确，当前主线在 post-alignment audit 后默认进入 maintenance mode，除非显式命中重开条件，否则不再把 execution-system 当成持续扩张中的 roadmap 主线。
- 已推进 `ES-F.F32`：冻结维护态下的 re-entry protocol。当前规则已明确：若未来要重开 execution-system 主线，必须先记录 concrete trigger、说明为什么现有维护态已不足、并且一次只为一个具体结构缺口或重复痛点重开，不允许回到无边界 backlog 式系统工作。
- execution-system 主线当前已形成更稳定的 maintenance-mode posture：core final-spec alignment 已 substantially landed，后续默认不再线性扩 execution-system slices；若继续推进，应先判断是否真的命中了 reopen conditions，而不是把 maintenance mode 当成继续造 backlog 的缓冲区。
- 已将 focus 从 `execution-system-spec-v1` 切回 `one-publish-refactor`，使 execution-system 真正进入 maintenance-mode posture。切换后 `test_check_active_consistency.py` 已修成动态读取当前 focus activity，不再把 execution-system 写死为测试前提；当前 `accept_active_ledger_v2.py`、`validate_focus_first.py` 与 unified runner 在业务 focus 下全部通过，说明“execution-system 维护态 + 业务主线恢复推进”已成立。
- 已继续推进 `one-publish-refactor / PR-E.P21`：新增 `src/hooks/useExecutionHistoryCardProps.ts`，把 `ExecutionHistoryCard` 的大段 props assembly 从 `src/App.tsx` 收口到独立 hook。这刀不动 publish runtime、不改 UI 结构，属于低风险前端 shell-collapse continuation；`pnpm typecheck`、`pnpm test`、`cargo check`（在 `src-tauri/`）均通过。
- 已完成 `one-publish-refactor / PR-E.P22`：提交 `0a6efd8` (`refactor: extract publish content section wiring`)。本刀连续把 `DotnetPublishCard`、`GenericProviderPublishCard`、`CommandImportResultCard`、`OutputLogCard`、`FailureGroupsCard`、`FailureGroupDetailCard` 的 props assembly 从 `src/App.tsx` 收口到独立 hooks，并新增 `src/components/layout/PublishContentSection.tsx` 接管右侧 publish content 区块渲染，进一步把 `App.tsx` 压回 layout shell。过程中修掉了 `getPresetText` 返回值缺少 `id` 的类型问题，以及一次把 `cargo check` 错跑到 repo 根目录的问题；最终 `pnpm typecheck`、`pnpm test`、`cargo check`（在 `src-tauri/`）均通过。
- 已完成 `one-publish-refactor / PR-E.P23`：提交 `0ef5d4e` (`refactor: extract sidebar panel shell`)。本刀新增 `src/components/layout/MainContentShell.tsx` 与 `src/components/layout/SidebarPanelShell.tsx`，把右侧主内容容器以及左右侧栏共同的 `p-2 + transition + CollapsiblePanel` 外壳从 `src/App.tsx` 抽出，让三栏都走显式 layout shell。过程中修掉了一次 `cn` 未使用的类型错误，以及再次提醒自己 `cargo check` 不能和前端命令串在 repo 根目录里跑；最终 `pnpm typecheck`、`pnpm test`、`cargo check`（在 `src-tauri/`）均通过。
- 已完成 `one-publish-refactor / PR-E.P24`：提交 `6b83b17` (`refactor: extract history diagnostics state`)。本刀新增 `src/hooks/useHistoryDiagnosticsState.ts`，把 history filter 本地状态、issue draft UI 状态、scoped execution history、provider options、snapshot paths、以及 failure-group 上游输入都从 `src/App.tsx` 收口到一个独立 hook，并继续复用现有 `useHistoryViewState`。过程中修掉了一次 `Repository | null` 窄化问题和一个 `useMemo` 未使用导入；最终 `pnpm typecheck`、`pnpm test`、`cargo check`（在 `src-tauri/`）均通过。
- 已完成 `one-publish-refactor / PR-E.P25`：提交 `161c4ea` (`refactor: extract diagnostics ui state`)。本刀新增 `src/hooks/useDiagnosticsUiState.ts`，把 `environmentLastResult`、`recentBundleExports`、`recentHistoryExports` 以及对应的 `trackBundleExport` / `trackHistoryExport` 从 `src/App.tsx` 收口到独立 hook，继续压缩 diagnostics 相关本地 UI 状态。过程中顺手清理了一次残留的 `EnvironmentCheckResult` 未使用导入；最终 `pnpm typecheck`、`pnpm test`、`cargo check`（在 `src-tauri/`）均通过。
- 已完成 `one-publish-refactor / PR-E.P26`：提交 `107ab1f` (`refactor: extract dialogs composition state`)。本刀新增 `src/hooks/useDialogsCompositionState.ts`，把 `environmentStatus` 推导、`useDialogDerivedState` 的 command-import/config 派生，以及最终 `useAppDialogsProps` 的大段装配从 `src/App.tsx` 收口到一个组合 hook。过程中为 `useAppDialogsProps.ts` 导出了参数类型，并将组合 hook 的输入改成显式 `Omit<...>`，避免把派生字段又要求调用方回传；最终 `pnpm typecheck`、`pnpm test`、`cargo check`（在 `src-tauri/`）均通过。
- 已完成 `one-publish-refactor / PR-E.P27`：提交 `c185bd0` (`refactor: extract layout shell state`)。本刀新增 `src/hooks/useLayoutShellState.ts`，把左右面板折叠状态、响应式宽度推导、以及 resize handler 从 `src/App.tsx` 收口到独立 hook。这样 `App.tsx` 连 layout bookkeeping 也进一步从组合层剥离。最终 `pnpm typecheck`、`pnpm test`、`cargo check`（在 `src-tauri/`）均通过。
- 已完成 `one-publish-refactor / PR-E.P28`：提交 `b54fe61` (`refactor: extract repository view state`)。本刀新增 `src/hooks/useRepositoryViewState.ts`，把 `selectedRepo` 推导、branch connectivity 刷新状态、以及 execution history holder 从 `src/App.tsx` 收口到独立 hook。这样 `App.tsx` 又少了一块 repo-facing view state，但仍未碰 publish runtime 本身。最终 `pnpm typecheck`、`pnpm test`、`cargo check`（在 `src-tauri/`）均通过。
- 已完成 `one-publish-refactor / PR-E.P29`：提交 `0fcf5a5` (`refactor: extract project execution state`)。本刀新增 `src/hooks/useProjectExecutionState.ts`，把 execution history 初始化加载/截断、rerun checklist 偏好持久化、`buildExecutionRecord`、`persistExecutionRecord`、以及 preset selection 的接线从 `src/App.tsx` 收口到独立 hook；同时将 `useRepositoryViewState` 收敛为只负责 `selectedRepo` 与 branch connectivity（不再同时持有 execution history），避免重复状态源。最终 `pnpm typecheck`、`pnpm test`、`cargo check`（在 `src-tauri/`）均通过。
- 已完成 `one-publish-refactor / PR-E.P30`：提交 `925bc2c` (`refactor: extract project shell state`)。本刀新增 `src/hooks/useProjectShellState.ts`，把 `projectInfo` 的生命周期、dotnet 项目扫描副作用、以及 Tauri drag-region 相关的窗口拖拽 wiring 从 `src/App.tsx` 收口到独立 hook。过程中为了避免重复状态源，保留 `useProjectExecutionState` 只负责 execution history / rerun checklist / preset selection，不把 project shell 逻辑混进去。最终 `pnpm typecheck`、`pnpm test`、`cargo check`（在 `src-tauri/`）均通过。
- 已为 `one-publish` 的 Phase 5 补了一次阶段判断：把 `PR-E.P21` 到 `PR-E.P30` 视为同一段 frontend shell-collapse wave，并把当前判断写回 `plans/one-publish-refactor-plan.md`、`roadmaps/one-publish-refactor-roadmap.md`、`tasks/one-publish-refactor-tasks.md`。当前结论是：`src/App.tsx` 已降到约 `953` 行，纯 view wiring / 壳层状态的大头基本已收走；继续拆之前应先判断是否还存在“非 publish runtime 且可独立验证”的最后一刀，否则应把这一波作为阶段性完成收口，而不是为追逐 `<800` 机械切更贴近 runtime 的薄片。
- 已完成 `one-publish-refactor / PR-E.P31`：提交 `20f94b6` (`refactor: extract provider presentation state`)。本刀新增 `src/hooks/useProviderPresentationState.ts`，把 available/resolved provider 派生、provider label 生成、RepositoryList 的 provider list mapping，以及 custom config patch helper 从 `src/App.tsx` 收口到独立 hook。当前 `src/App.tsx` 已降到约 `928` 行，剩余块更明显地集中在 publish execution orchestration 与少量高耦合接线上。
- 已将 `one-publish` 当前判断推进到 `PR-E.P33`：把 `PR-E.P21` 到 `PR-E.P31` 视为同一段 frontend shell-collapse wave，并明确当前推荐动作是“阶段性收口判断优先”，而不是继续为了追逐 `<800` 行数机械拆更贴近 publish runtime 的薄片。相关判断已同步到 `plans/one-publish-refactor-plan.md`、`roadmaps/one-publish-refactor-roadmap.md`、`tasks/one-publish-refactor-tasks.md` 与 `ACTIVE.md`。
- 已继续把该判断固化到 `PR-E.P34`：当前默认策略改为“在没有新低风险切片前，不再继续拆纯前端壳层薄片；若要推进下一步，先做 publish runtime 邻接层 / orchestration boundary 的进入条件设计”。也就是说，前端 shell-collapse 这一波现在被视为阶段性完成状态，而不是无限继续切小片。
- 已进一步推进到 `PR-E.P35`：把 publish runtime 邻接层的进入条件也写进 `ACTIVE.md`、`plans/one-publish-refactor-plan.md`、`roadmaps/one-publish-refactor-roadmap.md`。当前门槛是：必须先明确单一入口点、明确本刀不碰边界、能做到单 commit 回滚、并继续守住 `pnpm typecheck` / `pnpm test` / `cargo check` 三条验证，否则不进入 runtime 邻接层。
- 已继续推进到 `PR-E.P36`：把 runtime-adjacent 第一刀的候选入口也定了下来。当前默认首选入口是 `usePublishExecution` 邻接层，而不是直接改 `publish.rs` / `commands/mod.rs`；首刀仍要求单点进入、单点回滚，并明确不同时碰 `useProfiles`、`useRepositoryActions`、dialogs wiring 等其他高耦合面。
- 已进一步推进到 `PR-E.P37`：把 runtime-adjacent 第一刀进一步收紧成 entry slice：默认只允许处理 `App.tsx -> usePublishExecution` 的 call-surface / 参数装配边界，不改 hook 内执行语义，也不碰 `publish.rs` / `commands/mod.rs`。如果这 4 条不能同时成立，就继续停留在文档边界设计阶段，不贸然进入 runtime phase。
- 已继续把 `PR-E.P37` 的 entry slice 补细成 contract surface：明确首刀优先围绕 `selectedRepoId`、`selectedRepo`、`activeProviderId`、`activeProviderParameters`、`selectedPreset`、`isCustomMode`、`customConfig`、`defaultOutputDir`、`projectInfo`、`pushRecentConfig`、`openEnvironmentDialog`、`setEnvironmentLastResult`、`buildExecutionRecord`、`persistExecutionRecord` 这组参数面做 call-surface-only 收口；若无法维持“只改调用面、不改 runtime semantics”，则继续停留在文档阶段。
- 已继续为下一步做准备：把这组 contract surface 再拆成两层写进 plan / roadmap / tasks——一层是继续留在 `App.tsx` 的核心输入状态面（repo/provider/preset/config/projectInfo），另一层是可优先收口的回调/副作用面（`pushRecentConfig`、`openEnvironmentDialog`、`setEnvironmentLastResult`、`buildExecutionRecord`、`persistExecutionRecord`）。这样 `PR-E.P38` 就不再只是抽象原则，而是可直接落成第一刀 call-surface-only 代码说明的前置文档。
- 已继续推进到 `PR-E.P39`：把第一刀建议变更集也写清楚了。当前推荐不是同时移动整组 contract surface，而是优先只收口回调/副作用层（`pushRecentConfig`、`openEnvironmentDialog`、`setEnvironmentLastResult`、`buildExecutionRecord`、`persistExecutionRecord`），暂时保留 repo/provider/preset/config/projectInfo 这组核心输入状态仍由 `App.tsx` 直接装配。这样第一刀更容易满足单点进入、单点回滚、且不改 runtime semantics。
- 已继续推进到 `PR-E.P40`：把这份建议变更集再落成更接近实现的代码切片说明。当前推荐形式不是立刻改 `usePublishExecution` 内部流程，而是若进入代码实现，优先新增一个更窄的 publish-execution call-surface helper / composition helper，只聚合那 5 个回调/副作用面。这样第一刀仍然属于 contract narrowing，不是 runtime relocation。
- 已继续推进到 `PR-E.P41`：把实现入口也写清楚了。当前推荐是：helper 放在前端 hooks/composition 层（可命名为 `usePublishExecutionCallSurface` 或同级 helper），`App.tsx` 仍直接装配 repo/provider/preset/config/projectInfo 这组核心输入状态，而 helper 只聚合 `pushRecentConfig`、`openEnvironmentDialog`、`setEnvironmentLastResult`、`buildExecutionRecord`、`persistExecutionRecord`，再把较窄 call-surface object 交给 `usePublishExecution`。第一刀里 `usePublishExecution` 最多只改参数接收面，不改内部行为分支。
- 已继续推进到 `PR-E.P42`：把实现草案边界也写清楚了。当前建议 helper 文件优先落在 `src/hooks/usePublishExecutionCallSurface.ts`；helper 不接收 repo/provider/preset/config/projectInfo 这组核心输入状态；`usePublishExecution` 第一刀只允许做参数签名调整、解构位置调整、变量改名等接线型改动。任何会触碰 invoke 顺序、cancel 路径、日志流、environment gate、publish spec 构造的修改，都视为超出第一刀边界。
- 已继续推进到 `PR-E.P43`：把第一刀的 implementation brief 也落到文档里了。当前最小变更集说明是：新增 `src/hooks/usePublishExecutionCallSurface.ts`，只收口 `pushRecentConfig`、`openEnvironmentDialog`、`setEnvironmentLastResult`、`buildExecutionRecord`、`persistExecutionRecord` 这 5 个回调/副作用面；`App.tsx` 调整为传入更窄对象；`usePublishExecution` 改成接收更窄的 call-surface object，但不做任何 runtime 行为改动，不改 tauri invoke payload / publish session lifecycle / cancel / log / environment / spec 语义。
- 已继续推进到 `PR-E.P44`：把真正开工前 checklist / acceptance brief 也写进 plan / roadmap / tasks 里了。当前要求是在进入代码前，先明确 helper 不能吞入核心输入状态、`usePublishExecution` diff 只允许签名/解构/引用级接线改动、`App.tsx` 不能引入第二状态源，并把 publish success / failure / cancelled 三条主路径的人肉核对作为 acceptance 门槛写清楚。
- 已继续推进到 `PR-E.P45`：把最终的 go/no-go 决策点也写出来了。当前明确要求是：只有当 helper 边界、`usePublishExecution` diff 边界、`App.tsx` 单一状态源约束、三条验证命令、以及 success/failure/cancelled 三条路径的人肉核对都满足时，才允许真正进入 `usePublishExecutionCallSurface` 代码实现；否则继续停在文档边界阶段。
- 已继续推进到 `PR-E.P46`：把这一整轮 runtime-adjacent planning 的阶段收口状态也明确成 `ready for go/no-go decision`。这意味着当前默认不再继续机械扩 planning 文档；后续若推进，要么人类明确 `go` 进入第一刀代码实现，要么维持当前结论不动。
- 已继续推进到 `PR-E.P47`：把“停止继续扩写 planning”也写成显式规则了。当前默认行为应是：没有明确 `go`，就不进入 `usePublishExecutionCallSurface` 代码实现；没有明确 `no-go/defer`，也不销毁当前结论，但同样不再继续自动扩写 planning slice。
- 已执行 `go` 并完成 `PR-E.P48` 代码落地：新增 `src/hooks/usePublishExecutionCallSurface.ts`，把 `pushRecentConfig`、`openEnvironmentDialog`、`setEnvironmentLastResult`、`buildExecutionRecord`、`persistExecutionRecord` 这 5 个回调/副作用面先收口成更窄的 call-surface object，再由 `App.tsx` 传给 `usePublishExecution`。`usePublishExecution` 仅改参数接收面与引用接线，不改内部执行分支；`pnpm typecheck`、`pnpm test`、`cargo check` 全部通过。提交：`2e7d0fe` (`refactor: narrow publish execution call surface`)。
- 已继续完成 `PR-E.P49` 代码落地：新增 `src/hooks/usePublishExecutionInput.ts`，把 `selectedRepoId`、`selectedRepo`、`activeProviderId`、`activeProviderParameters`、`selectedPreset`、`isCustomMode`、`customConfig`、`defaultOutputDir`、`projectInfo`、`presets`、`specVersion` 这组核心输入状态面再收口成 `publishExecutionInput`，由 `App.tsx` 传给 `usePublishExecution`。`usePublishExecution` 本刀仍只改参数接收与引用解构，不改内部执行语义；`pnpm typecheck`、`pnpm test`、`cargo check` 全部通过。提交：`9b5c3bd` (`refactor: narrow publish execution input surface`)。
- 之后又补齐了第二刀的 git 落地：最终 HEAD 为 `94a8a9e` (`refactor: narrow publish execution input surface`)；当前账本以该提交为准。到此为止，runtime-adjacent 已有两刀真实代码落地：第一刀 `usePublishExecutionCallSurface`，第二刀 `usePublishExecutionInput`。两刀都保持在参数接收面/引用接线范围内，未触碰 `publish.rs`、`commands/mod.rs`、也未改变 `usePublishExecution` 的执行分支与 success/failure/cancelled / cancel / log / environment / spec 语义。
- 已继续完成 `PR-E.P51` 代码落地：新增 `src/hooks/useDotnetPublishSelection.ts`，把 dotnet 相关的 `getCurrentConfig`、preview command 生成、recent config key 推导从 `src/hooks/usePublishExecution.ts` 收口到独立 hook。`usePublishExecution` 本刀仍只改纯派生与引用接线，不改内部执行分支；`pnpm typecheck`、`pnpm test`、`cargo check` 全部通过。提交：`a3c5a51` (`refactor: extract dotnet publish selection`)。
- 已继续完成 `PR-E.P52` 代码落地：新增 `src/hooks/usePublishSpecBuilder.ts`，把 `executePublish` 里 provider-specific 的 spec 构造继续抽到独立 builder，`usePublishExecution` 现在只保留 orchestration entry 与调用该 builder 的薄层。`pnpm typecheck`、`pnpm test`、`cargo check` 全部通过。提交：`0ff4e14` (`refactor: extract publish spec builder`)。本刀仍未触碰 `publish.rs` / `commands/mod.rs`，也未修改 success/failure/cancelled、cancel、log、environment、spec 语义。
- 已继续完成 `PR-E.P53` 代码落地：新增 `src/hooks/usePublishFailureFeedback.ts`，把 publish failure reason 到 toast 文案/描述的映射从 `src/hooks/usePublishExecution.ts` 收口为纯 helper。`usePublishExecution` 本刀仍只改纯映射与引用接线，不改内部执行分支；`pnpm typecheck`、`pnpm test`、`cargo check` 全部通过。提交：`eb26e6b` (`refactor: extract publish failure feedback`)。
- 已继续完成 `PR-E.P54` 代码落地：新增 `src/hooks/useCancelPublishFeedback.ts` 与 `src/hooks/usePublishExecutionTypes.ts`，把 cancel failure feedback 的 toast 映射从 `src/hooks/usePublishExecution.ts` 抽成纯 helper，并顺手把 `TranslationMap` 类型独立出来供相关 helper 复用。`pnpm typecheck`、`pnpm test`、`cargo check` 全部通过。提交：`187e934` (`refactor: extract cancel publish feedback`)。本刀依然未触碰 `publish.rs` / `commands/mod.rs`，也未改变 usePublishExecution 的执行分支与 runtime 语义。
- 已继续完成 `PR-E.P55` 代码落地：新增 `src/hooks/usePublishLogStream.ts`，把 `provider-publish-log` 事件订阅与 `outputLog` state 从 `src/hooks/usePublishExecution.ts` 收口成独立 hook。`usePublishExecution` 仍只通过该 hook 取 `outputLog` / `setOutputLog`，没有改变日志语义或事件顺序。`pnpm typecheck`、`pnpm test`、`cargo check` 全部通过。提交：`174f311` (`refactor: extract publish log stream`)。
- 已继续完成 `PR-E.P56` 代码落地：新增 `src/hooks/usePublishUiState.ts`，把 `isPublishing`、`isCancellingPublish`、`publishResult`、`lastExecutedSpec`、`currentExecutionRecordId`、`releaseChecklistOpen`、`artifactActionState` 这组 publish 相关本地 UI state 从 `src/hooks/usePublishExecution.ts` 收口到独立 hook。`pnpm typecheck`、`pnpm test`、`cargo check` 全部通过。提交：`e3f2a70` (`refactor: extract publish ui state`)。本刀仍未触碰 `publish.rs` / `commands/mod.rs`，也未改变 usePublishExecution 的执行分支与 runtime 语义。
- 已继续推进到 `PR-E.P46`：把这轮 runtime-adjacent planning 的阶段收口结论也写清楚了。当前判断是：frontend shell-collapse wave 与 runtime-adjacent first-cut planning 都已经形成闭环，后续默认状态应改为“planning complete, waiting for go/no-go”，除非人类明确要求继续进入第一刀代码实现，否则不再机械扩写 planning slice。
- 截至今晚，`one-publish` 的 frontend shell-collapse wave 被明确视为阶段性完成：`PR-E.P21` 到 `PR-E.P31` 连续完成了 publish card props assembly、publish content/layout shells、history/diagnostics/dialogs/layout/repository/project/provider 相关组合层状态与装配的收口，`src/App.tsx` 已降到约 `928` 行；后续默认不再为追逐 `<800` 机械拆纯前端壳层薄片，除非发现最后一块明确独立且不触碰 publish runtime 的低风险切片。
- 已落地 `docs/execution-system-maintenance-guardrails.md` 作为 execution-system 的正式维护守则。该文档把 reopen trigger、closeout state explicitness、ACTIVE 防回弹、动态化 smoke、advisory 防噪声化等 5 条 guardrails 明确成可执行维护规则，并已补进 execution-system activity 的 `last_artifact` 恢复指针。
- 已推进 `ES-F.F33`：把 `acceptance / summary / closeout` 一体化操作链的第一版实施计划冻结进 execution-system spec/tasks/roadmap/ACTIVE。当前计划明确只做薄整合：共享结果模型、structured summary、acceptance 复用、closeout-ready gate、chain smoke；并明确保持 raw output 可见、advisory 不升级成 hard-fail。
- 已开始实施一体化操作链第一刀：`scripts/run_execution_system_checks.py` 现在先构建结构化 `ExecutionSystemSummary`，再渲染现有 footer；`scripts/test_run_execution_system_checks.py` 也已切到测试 `build_summary()+summary_footer()` 组合。当前 unified runner、acceptance、runner smoke 全部通过，说明“结构化 summary 结果源”已落地且未改变外部行为。
- 已继续实施第二刀：`scripts/accept_active_ledger_v2.py` 现在会直接复用 `collect_summary()` 的 execution-system-suite 结果语义，而不是重新把 runner 当黑盒解析。当前 acceptance 输出仍保持原有人类可读形态，但 execution-system-suite 这一路的 hard-fail/advisory 状态来源已经统一。
- 已继续实施第三刀：新增 `scripts/check_closeout_ready.py`，并让 `scripts/complete_slice.sh prepare` 在统一 checker suite 之后显式跑 closeout-ready gate。当前 closeout-ready 语义已要求：hard-fail suite 通过、focus activity 为 roadmap、并且 `current_slice_id` / `next_slice_id` / `last_commit` / `last_validation` 全部存在；同时新增 `scripts/test_check_closeout_ready.py` 作为最小 smoke。
- 已继续实施第二刀的深化：`accept_active_ledger_v2.py` 现在通过 `collect_summary()` 直接复用 execution-system-suite 的结构化结果，而不是把 runner 当黑盒再推导一遍；同时第三刀也已成立，closeout-ready gate 与 acceptance / summary / checker 三者现在形成同一条 operator workflow 语义链。
- 已补链路级 smoke：新增 `scripts/test_execution_workflow_chain.py`，在真实当前 workspace 状态下串测 `run_execution_system_checks.py` -> `check_closeout_ready.py` -> `accept_active_ledger_v2.py` 这条主路径。当前 `pass + advisory + closeout-ready + acceptance ok` 组合已具备整链路回归保护。
- 已完成 `ET-A.A1`：新增 `docs/execution-system-testing-inventory.md`，把当前 execution-system 测试资产按 layout / semantics / workflow / governance 四层正式盘点，并明确缺口。当前 inventory 结论已把下一步推荐切到 `ET-C.C1`，即优先补 hard-fail path workflow test，而不是继续停留在纯计划阶段。
- 已开始实施 `ET-C.C1`：新增 `scripts/test_execution_workflow_hard_fail.py`。当前该测试会在进程内构造 hard-fail summary，并验证同一条链路下 `summary_footer`、`check_closeout_ready.py` 与 `accept_active_ledger_v2.py` 三者都会进入失败语义；这意味着 `runner fail -> closeout-ready fail -> acceptance fail` 这条关键负路径已具备回归保护。
- 已继续实施 `ET-C.C2`：新增 `scripts/test_execution_workflow_policy_gate.py`。当前该测试会在进程内构造 `focus-first -> POLICY_GATE` 场景，并验证 acceptance 会输出 `ACTIVE_LEDGER_V2_ACCEPTANCE_OK_WITH_POLICY_GATES` 而不是失败；这意味着 policy-gate 路径也已进入 workflow matrix 回归保护。
- 已继续实施 `ET-C.C3`：新增 `scripts/test_execution_workflow_closeout_blocked.py`。当前该测试会构造 hard-fail suite 已通过、但 `last_validation` 缺失导致 closeout-ready 被阻断的场景，并验证 `check_closeout_ready.py` 会返回 `CLOSEOUT_READY_FAILED`；这意味着 closeout-blocked 路径也已进入 workflow matrix 回归保护。
- 已继续实施 `ET-C.C4`：新增 `scripts/test_execution_workflow_focus_switch.py`。当前该测试会验证从 execution-system maintenance focus 切回业务 focus 后，`test_check_active_consistency.py`、`validate_focus_first.py` 与 `accept_active_ledger_v2.py` 都保持绿灯，不再因旧 focus 假设而误报；这意味着 focus-switch 路径也已进入 workflow matrix 回归保护。
- 已开始实施 `ET-D.D1`：新增 `scripts/check_execution_system_maintenance_mode.py` 与 `scripts/test_execution_system_maintenance_mode.py`。当前该检查会验证 `execution-system-spec-v1` 处于 `parked`/`autopilot=false`、业务 focus 为 `one-publish-refactor` 且 acceptance 仍然为绿；这意味着 maintenance-mode consistency 已进入自动检查范围。
- 已继续实施 `ET-D.D2`：新增 `scripts/check_execution_system_governance_consistency.py` 与 `scripts/test_execution_system_governance_consistency.py`。当前该检查会轻量核对 spec / roadmap / tasks / ACTIVE 是否同时保留 maintenance mode、reopen conditions、re-entry protocol 与 parked activity state 这些关键治理语义；这意味着 execution-system 的治理层也已进入自动一致性检查范围。
- 已实施 `ET-E.E1`：新增 `scripts/run_execution_system_full_tests.py` 作为 execution-system 全量测试统一入口。当前 full-test runner 会汇总 maintenance/governance、workflow matrix、semantic/smoke、acceptance 与 unified runner 等 15 项测试，并输出 `EXECUTION_SYSTEM_FULL_TEST_SUMMARY`；当前实测结果为 `failed: 0` / `EXECUTION_SYSTEM_FULL_TESTS_OK`。
- execution-system 主线已继续推进到 `ES-F.F25` 之后；截至本次压缩前，workspace 已实际落地的关键执行系统能力包括：两条 hard-fail checker（ACTIVE field checker、migrated task slice schema checker）、两条对应 smoke、统一 runner、workflow summary footer、oversized migration slice advisory 及其 smoke，以及 closeout artifact 中显式的 `slice_state: closed_out`。
- 当前工作判断：与“执行系统最终规范 v1”相比，最大已补齐缺口之一是把 `closed_out` 从文档状态机推进成真实工件字段；后续若继续推进，下一优先缺口是把 `validated` 也从隐含验证列表进一步收口成更明确的工件/流程语义。
- 当前执行真相在压缩前为：focus activity=`execution-system-spec-v1`，current slice=`ES-F.F25`，next slice=`ES-F.F26`，最近已验证 commit=`f4829c2`；统一校验入口 `scripts/run_execution_system_checks.py` 通过，summary footer smoke 与 closeout-state smoke 也通过。
