# 2026-03-13

- 修复了 workspace 执行状态漂移问题：把 `ACTIVE.md` 收紧为当前执行切片的唯一真相源，不再保留大段历史流水。
- 将 `one-publish` 当前状态正规化为：最新 repo commit 为 `eedacbf`，前端 hooks 拆分到 `PR-C.C4` 为止；下一切片是 `PR-D.D1`，目标为右侧 UI card 的纯组件抽取。
- 瘦身 `SESSION-STATE.md`，移除重复的 phase/task/next-step 执行字段，仅保留策略、WAL、通知与切片切换规则。
- 新增一致性检查脚本：`/Users/erictao/.openclaw/workspace/scripts/check_active_consistency.py`。
- 更新 `AGENTS.md`：进度回复前必须用 repo 事实核对 `ACTIVE.md:last_commit`；切片切换必须按原子 checklist 收口。
- 主人已确认：直接落地一套完善方案，防止后续再次出现执行状态不一致。
- heartbeat 已接入一致性检查：Active Work Check 前先跑 `/Users/erictao/.openclaw/workspace/scripts/check_active_consistency.py`。
- 继续推进 `one-publish` 的 `PR-D`：已提交 `64fee79` (`refactor: split output log card`)，把右侧输出日志区域抽成 `src/components/publish/OutputLogCard.tsx`。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-D.D2`，下一步优先抽 `ExecutionHistoryCard`。
- 继续推进 `one-publish` 的 `PR-D`：已提交 `4db4e62` (`refactor: split execution history card`)，把执行历史区域抽成 `src/components/publish/ExecutionHistoryCard.tsx`。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-D.D3`，下一步优先抽 `FailureGroupsCard`。
- 继续推进 `one-publish` 的 `PR-D`：已提交 `61bec77` (`refactor: split failure groups card`)，把失败分组区域抽成 `src/components/publish/FailureGroupsCard.tsx`。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-D.D4`，下一步优先抽 `FailureGroupDetailCard`。
- 主人明确要求：继续推进时要更主动，不要等他来催；完成一个切片后直接汇报结论即可。
- 主人已确认：将“默认自主推进协议”正式固化进 `AGENTS.md` / `HEARTBEAT.md`，之后只要 `ACTIVE.md` 有清晰安全的下一步，就默认继续推进。
- 主人已确认：每次 roadmap 切片完成且验证通过后，允许用飞书主动通知；通知机制改为 queue + dedupe + heartbeat compensation。
- 通知机制已升级为 queue + flush + ack 三段式：先入队、发送前转 inflight、发送成功后再 ack 为 sent，降低漏发和重复发的概率。
- 主人已确认：从现在开始，roadmap 切片完成后的默认收口路径切换到 `scripts/complete_slice.sh`，不再走旧的人肉收口路径。
- 已补齐 `complete_slice.sh` 到 queue + flush 阶段，并补了 inflight 回滚脚本 `scripts/requeue_inflight_notifications.py`；后续切片完成时应以 `complete_slice.sh` 输出的通知 payload 为立即发送真相。
- 继续补强通知收口：`complete_slice.sh` 现在会把 flushed payload 落到 `memory/last-slice-notification.json`；新增 `scripts/get_inflight_notification.py` 作为 heartbeat / 恢复时读取 inflight 通知的统一入口。
- 已把飞书发送本身纳入统一入口协议：标准顺序变为 `complete_slice.sh prepare` -> `complete_slice.sh payload` -> 发送飞书 -> `complete_slice.sh ack <dedupe_key>`；发送失败则 `complete_slice.sh fail`。
- 主人已确认：roadmap 切片的完成定义升级为“验证通过 + commit + ACTIVE 切换 + 通知已入队/发送”，缺少通知 closeout 不得宣布切片完成；新增 `scripts/check_slice_closeout.py` 作为验证脚本。
- 已开始落地 completed slice artifact 模型：新增 `memory/last-slice-closeout.json` 与 `scripts/create_slice_closeout.py`，后续 queue / payload / closeout 检查统一围绕“刚完成切片”的冻结对象，而不再从 `ACTIVE.md` 反推完成对象。
- 已将 `scripts/create_slice_closeout.py` 改为显式传参版，通知 artifact 不再直接读取 `ACTIVE.md`；`SESSION-STATE.md` 中的 closeout / dedupe 口径也统一改为 `completed_slice_id`。
- 已补完 `complete_slice.sh` 对新接口的调用：现在由 `complete_slice.sh prepare` 先从 `ACTIVE.md` 读取字段，再以显式参数传给 `create_slice_closeout.py`；通知下游脚本不再直接读取 `ACTIVE.md` 生成完成对象。
- 已修复两个边角一致性问题：新增 `scripts/normalize_notifications_state.py` 把历史通知记录统一迁移到 `completed_slice_id` 字段；`ack_slice_notification.py` 现在禁止直接调用，只允许通过 `complete_slice.sh ack` 完成最终确认，避免缓存清理被绕过。
- 继续推进 `one-publish` 的 `PR-D`：已提交 `9d4b567` (`refactor: split failure group detail card`)，把失败组详情区域抽成 `src/components/publish/FailureGroupDetailCard.tsx`。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-D.D5`，下一步优先抽 `DotnetPublishCard`。
- 继续推进 `one-publish` 的 `PR-D`：已提交 `e394a74` (`refactor: split dotnet publish card`)，抽出 `src/components/publish/DotnetPublishCard.tsx`。
- 继续推进 `one-publish` 的 `PR-D`：已提交 `2fda9b6` (`refactor: split generic provider publish card`)，抽出 `src/components/publish/GenericProviderPublishCard.tsx`，并清理 `App.tsx` 的对应 inline block。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- Phase 4（UI cards split）已全部完成，`ACTIVE.md` 已切换到 Phase 5 准备切片 `PR-E.P1`（先定义最小落刀）。
- 已把 Phase 5 的当前推荐切片与第一刀定义同步回 `roadmaps/one-publish-refactor-roadmap.md` / `tasks/one-publish-refactor-tasks.md`，避免执行真相仍停留在 `PR-D.D6`。
- PR-E.P1 已明确：Phase 5 第一刀先做前端 shell collapse，不先动 `src-tauri/src/commands/mod.rs` 里的 publish runtime；P2 优先从 `App.tsx` 剩余的配置/筛选/导入装配逻辑里选最独立的一块下刀。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `e1aa7eb` (`refactor: extract history preset shell logic`)，把历史筛选预设装配逻辑从 `App.tsx` 再收口一刀。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P3`，下一步优先抽命令导入映射/反馈装配逻辑。
- 继续推进 `one-publish` 的 `PR-D`：已提交 `e394a74` (`refactor: split dotnet publish card`)，把 .NET 发布主卡区域抽成 `src/components/publish/DotnetPublishCard.tsx`。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-D.D6`，下一步优先抽 `GenericProviderPublishCard`。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `bd093ab` (`refactor: split history presets and import result card`)。
- 本刀内容：新增 `src/hooks/useHistoryPresets.ts`，把历史筛选预设/每日分诊预设的持久化与保存/删除操作从 `App.tsx` 抽出；新增 `src/components/publish/CommandImportResultCard.tsx`，把命令导入结果展示卡片抽出。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P3`，下一步继续评估 `handleCommandImport` 或 preview/config 装配的独立抽离机会。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `d111402` (`refactor: extract command import shell logic`)。
- 本刀内容：新增 `src/hooks/useCommandImport.ts`，把命令导入参数映射、反馈状态和 toast 提示从 `App.tsx` 抽出；`App.tsx` 改为只保留 hook 接线。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P4`，下一步优先评估 dotnet preview/config 组装逻辑的独立抽离机会。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `aa75845` (`refactor: move dotnet preview logic into publish hook`)。
- 本刀内容：把 dotnet 的 config 计算与 preview command 组装从 `src/App.tsx` 收回 `src/hooks/usePublishExecution.ts`；`App.tsx` 只消费 hook 暴露出的 `dotnetPublishPreviewCommand`。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P5`，下一步继续评估剩余前端装配块的独立抽离机会。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `f3fc90e` (`refactor: extract scoped config persistence`)。
- 本刀内容：新增 `src/hooks/useScopedConfigs.ts`，把最近/收藏配置的 localStorage 持久化与 repo 作用域迁移逻辑从 `App.tsx` 抽出；`App.tsx` 改为只消费 hook 暴露的键列表和操作函数。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P6`，下一步优先评估 rerun checklist、clipboard/failure actions 或 dialog wiring 中最独立的一块。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `e095f13` (`refactor: split rerun checklist dialog`)。
- 本刀内容：新增 `src/components/publish/RerunChecklistDialog.tsx`，把重跑前确认清单的弹窗 UI 与交互接线从 `App.tsx` 抽出；`App.tsx` 只保留状态与回调传递。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P7`，下一步优先评估 clipboard/failure actions 或剩余 dialog wiring 的独立抽离机会。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `d08cd10` (`refactor: split quick create profile dialog`)。
- 本刀内容：新增 `src/components/publish/QuickCreateProfileDialog.tsx`，把快速创建发布配置的整块弹窗 UI 与交互接线从 `App.tsx` 抽出；`App.tsx` 只保留状态和回调传递。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P8`，下一步优先评估 clipboard/failure actions 是否值得抽成 hook。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `32dae48` (`refactor: extract history action helpers`)。
- 本刀内容：新增 `src/hooks/useHistoryActions.ts`，把 clipboard copy、Issue draft、handoff snippet、snapshot 打开等历史/失败操作辅助逻辑从 `App.tsx` 抽出；`App.tsx` 只保留 hook 接线。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P9`，下一步优先评估剩余 dialog wiring 或 rerun/history 交互装配还能否继续收口。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `41992cd` (`refactor: extract app dialogs shell`)。
- 本刀内容：新增 `src/components/layout/AppDialogs.tsx`，把底部一组 dialogs 的渲染与接线从 `App.tsx` 收成独立 shell；`App.tsx` 只保留状态与回调透传。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P10`，下一步优先评估 environment/settings/config 相关装配是否还能继续收口。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `ffc7ba2` (`refactor: extract app dialog controls`)。
- 本刀内容：新增 `src/hooks/useAppDialogs.ts`，把设置/快捷键/命令导入/配置/环境检查弹窗的开关状态与公共打开/关闭回调从 `App.tsx` 抽出；`App.tsx` 改为只消费 hook 暴露的状态与动作。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P11`，下一步优先评估 settings/environment/config 相关剩余接线还能否继续收口。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `f26d098` (`refactor: extract rerun flow logic`)。
- 本刀内容：新增 `src/hooks/useRerunFlow.ts`，把 rerun checklist 的打开/关闭、待重跑记录、确认逻辑与直接重跑逻辑从 `App.tsx` 抽出；`App.tsx` 只保留 hook 接线。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P12`，下一步继续评估 settings/environment/config 相关剩余接线是否还能再收口一刀。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `1d5f363` (`refactor: extract environment status helper`)。
- 本刀内容：新增 `src/hooks/useEnvironmentStatus.ts`，把环境检查结果到 UI 状态（unknown/ready/warning/blocked）的推导从 `App.tsx` 抽出；`App.tsx` 改为只消费 hook 返回值。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- 提交时一度遇到瞬时 `.git/index.lock` 残留；确认锁文件已消失后重试提交成功。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P13`，下一步继续评估 settings/environment/config 相关剩余接线是否还能再收口一刀。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `f8c7b9a` (`refactor: extract dialog derived state`)。
- 本刀内容：新增 `src/hooks/useDialogDerivedState.ts`，把 dialogs 依赖的 `commandImportProjectPath` 与 `currentConfigParameters` 等派生状态从 `App.tsx` 抽出；`App.tsx` 改为只消费 hook 返回值。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P14`，下一步继续评估 settings/environment/config 相关剩余接线是否还能再收口一刀。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `47cb691` (`refactor: extract recoverable spec helpers`)。
- 本刀内容：新增 `src/hooks/useRecoverableSpec.ts`，把历史记录中的可恢复 spec 解析、编辑器回填、recent config key 推导从 `App.tsx` 抽出；`App.tsx` 改为只消费 hook 返回的 helper。
- 验证通过：`pnpm typecheck`、`pnpm test`。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P15`，下一步继续评估 settings/environment/config 相关剩余接线是否还能再收口一刀。
- 继续推进 `one-publish` 的 `PR-E`：已提交 `0c6685a` (`refactor: extract project scan hook`)。
- 本刀内容：新增 `src/hooks/useProjectScanner.ts`，把项目扫描与错误提示逻辑从 `App.tsx` 抽出；`App.tsx` 改为只保留 hook 接线。
- 验证通过：`pnpm typecheck`、`pnpm test`、`cargo check`（注意 Rust 校验需要在 `src-tauri/` 下执行）。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P17`，下一步继续评估 environment/checklist/config dialog 的剩余接线是否还能再收口一刀。
- 通知链路生产闭环已实际打通一次：对 `one-publish:PR-E.P7:e095f13` 完成了 pending -> inflight -> 飞书发送 -> ack -> sent，全链路已验证可用。
- `scripts/complete_slice.sh` 已升级为默认切片收口入口，当前至少负责 consistency check + queue + flush，并输出可直接发送的通知 payload；发送成功后需立刻执行 `scripts/ack_slice_notification.py <dedupe_key>`。
- 新增恢复脚本 `scripts/requeue_inflight_notifications.py`，用于发送失败后将 inflight 通知回滚到 pending，避免消息悬空。
- 当前 `one-publish` 活动状态已推进到 `PR-E.P7`，最新已验证 commit 为 `e095f13` (`refactor: split rerun checklist dialog`)；下一切片为 `PR-E.P8`。
- 已修复 `ACTIVE.md` 中残留的旧检索字段（旧的 `PR-E.P3` / `PR-D.D6` / `eedacbf` 文案），当前账本与 repo 事实一致。
- 主人要求将 `SESSION-STATE.md` 的有效内容完全并入 `ACTIVE.md`，并把相关规则改成只认 `ACTIVE.md`。
- 已完成状态文件收口：`ACTIVE.md` 现在同时承载执行切片、活动决策、通知策略与 WAL 约束；`AGENTS.md`、`HEARTBEAT.md`、`memory/working-buffer.md`、`scripts/check_active_consistency.py` 已改为只依赖 `ACTIVE.md`。
- `SESSION-STATE.md` 已退役为占位说明文件，只保留“改看 ACTIVE.md”的迁移提示。
- 一致性检查已通过：`scripts/check_active_consistency.py` 返回 `CONSISTENCY_CHECK_OK`。
- 主人要求开始实施三层账本模型，我已创建 `roadmaps/active-ledger-v2-roadmap.md` 与 `tasks/active-ledger-v2-tasks.md`，把该改造正式纳入 roadmap + tasks 执行。
- 已落地 `ACTIVE.md v2` 第一版骨架：加入 `Ledger meta`、`Activity index`、`Activities`，并显式引入 `current_focus_activity_id=active-ledger-v2` 与 `focus-first` 语义。
- 已保留两个 activity：当前 focus 为 `active-ledger-v2`，`one-publish-refactor` 作为非 focus 活动暂时并存，验证多活动账本基本形态。
- 已新增 `scripts/active_ledger.py` parser skeleton，支持读取 ledger meta、activity index、activity blocks 与 current focus。
- 已改造 `scripts/check_active_consistency.py` 为 focus-aware 版本，不再依赖全文件首个单任务字段；校验通过，输出 focus activity 为 `active-ledger-v2`。
- 已同步更新 `AGENTS.md` 与 `HEARTBEAT.md`：恢复/执行语义改为先读 ledger meta，再读 current focus activity。
- 继续推进三层账本模型：已把 `scripts/active_ledger.py` 补成统一 parser 雏形，新增 `list_activities` / `get_activity` / `get_current_focus_activity` / `list_runnable_activities` 等访问能力。
- 已改造 `scripts/complete_slice.sh`：prepare 现在从 current focus activity 读取 `activity_id/type/repo/current_slice_id/next_slice_id/last_commit/last_validation`，不再依赖扁平 `ACTIVE.md` 全局字段。
- 已改造 closeout 相关脚本：`create_slice_closeout.py`、`queue_slice_notification.py`、`check_slice_closeout.py` 现在开始带 `activity_id` / `activity_type`，roadmap dedupe 口径切到 `activity_id + completed_slice_id + commit`。
- 将 `active-ledger-v2` 当前切片推进到 `AL-B.B1`，当前目标改为 parser + focus-aware closeout 适配。
- 已修复一次 focus activity 漂移：`active-ledger-v2` 的 `last_commit` 从旧值 `5cd5077` 更新为当前 workspace HEAD `c6a9d76`；一致性检查重新通过。
- workspace repo 又继续推进到 `c755141`（`refactor: make closeout focus-activity aware`）：`active_ledger.py` 增加统一 accessors，`complete_slice.sh` / closeout / queue 链路开始按 focus activity 取值，多活动下不再默认读全局首字段。
- 由于 `ACTIVE.md` 里 `active-ledger-v2` 仍停在旧 commit，heartbeat 再次先触发一致性失败；已修正账本到最新 workspace 事实，并把当前 focus slice 顺延到 `AL-C.C1`。
- 本轮未执行 `complete_slice.sh prepare` 真队列，因为当前切片尚未完成，避免污染通知状态；仅完成静态校验与 parser smoke test。
- 继续推进 `active-ledger-v2`：已把 `check_active_consistency.py` 升级为三层校验（ledger / activity / focus）。
- checker 现在会分别校验：Ledger meta 完整性、Activity index 与 block 一致性、activity 通用字段、按 type 的字段约束，以及 focus activity 的 repo/commit/slice 严格校验。
- 已为多活动账本补入一个真实 `waiting` 类型样本 activity：`waiting-ledger-review`，用于验证非 roadmap activity 的 schema 与 checker 行为。
- `active_ledger.py` 的 `list_runnable_activities()` 已补强：waiting / blocked / 非 autopilot 活动不会被误判成可自动推进。
- 本轮中途再次发现 focus 漂移：workspace HEAD 为 `3ac9533` (`chore: repair active-ledger focus state`)，已把 `active-ledger-v2.last_commit` 修正为 `3ac9533`。
- 最新一致性检查已通过：`CONSISTENCY_CHECK_OK`，当前账本统计为 `activity_count=3`、`runnable_activity_count=1`。
- 继续推进 `active-ledger-v2` 的 Phase 4：已将 `notifications-state.json` 规范化到 `version=2`，并为历史 sent 记录补做 best-effort 的 `activity_id/activity_type` 推断。
- 已升级通知辅助脚本默认状态结构到 v2：`send_slice_notification_payload.py`、`flush_slice_notifications.py`、`requeue_inflight_notifications.py`、`get_inflight_notification.py`、`ack_slice_notification.py` 现在都兼容 activity-aware 字段。
- 已新增真实 `simple` 类型样本 activity：`simple-ledger-docs`，当前账本已同时覆盖 `roadmap + waiting + simple` 三种 activity 类型。
- 静态通知链路 smoke test 正常：无 inflight 时 `get_inflight_notification.py` 返回 `NO_INFLIGHT_NOTIFICATIONS`，无 closeout 时 `send_slice_notification_payload.py` 返回 `NO_SLICE_CLOSEOUT`。
- 本轮再次修正 focus 漂移：因上一轮 commit 导致 workspace HEAD 更新到 `49b8736`，已同步修正 `active-ledger-v2.last_commit` 为 `49b8736`。
- 最新一致性检查再次通过：`activity_count=4`、`runnable_activity_count=1`，当前 focus slice 为 `AL-D.D1`。
- 继续推进收口段：已新增 `scripts/set_focus_activity.py`，支持显式切换 `ACTIVE.md` 的 `current_focus_activity_id` / `default_reply_activity_id`。
- 已新增 `scripts/validate_focus_first.py`，用于验证“只有 current focus 且可运行的 activity 才能进入自动执行集合”。
- 已收紧 `HEARTBEAT.md` 与 `AGENTS.md`：明确只有 current focus activity 可自动推进；non-focus activities 只用于恢复、提醒、展示状态，不得在 heartbeat 中擅自推进。
- 已做一次真实 focus switch smoke test：切到 `one-publish-refactor` 后，`validate_focus_first.py` 按预期失败（因为它 `autopilot=false`）；切回 `active-ledger-v2` 后，`check_active_consistency.py` 与 `validate_focus_first.py` 均恢复通过。
- 当前 focus slice 已推进到 `AL-E.E1`，说明多活动账本的 focus-first 执行语义已经从规则落到可验证工具。
- 已新增 `docs/active-ledger-v2.md` 作为 ACTIVE Ledger v2 使用说明，覆盖三层账本模型、activity types、focus-first workflow、closeout 规则与恢复路径。
- 已新增 `scripts/accept_active_ledger_v2.py` 作为正式验收清单执行器；在 focus 仍为 `active-ledger-v2` 时运行通过，结果为 `ACTIVE_LEDGER_V2_ACCEPTANCE_OK`。
- `active-ledger-v2` 已完成收官并标记为 `done`；focus 已显式切回 `one-publish-refactor`，业务主线恢复。
- 切回后的一致性检查正常：`focus_activity_id=one-publish-refactor`，`activity_count=4`，`current_slice_id=PR-E.P15`，`next_slice_id=PR-E.P16`。
- 当前 `runnable_activity_count=0` 是符合设计的：`one-publish-refactor` 作为业务 focus 目前 `autopilot=false`，因此不会被 heartbeat 自动推进，避免擅自恢复编码执行。
- 主人已明确要求把 `one-publish-refactor` 设为自动运行；已将该 activity 的 `autopilot` 改为 `true`。
- 更新后校验通过：`focus_activity_id=one-publish-refactor`，`runnable_activity_count=1`，`validate_focus_first.py` 返回 `FOCUS_VALIDATION_OK`。
- 已继续推进 `one-publish` 当前切片 `PR-E.P15`：提交 `35d71a0` (`refactor: extract provider runtime hook`)。
- 本刀内容：新增 `src/hooks/useProviderRuntime.ts`，把 Provider 列表加载、schema 缓存、当前 provider 派生值、provider 参数写回从 `src/App.tsx` 抽出；`App.tsx` 改为只消费 hook 暴露值。
- 验证通过：`pnpm typecheck`、`pnpm test`、`cargo check`。
- `ACTIVE.md` 已切换到下一切片 `PR-E.P16`，下一步优先评估 environment/checklist/config dialog 相关剩余接线还能否继续收口。
- 已继续推进 `one-publish` 的 `PR-E`：提交 `cc47258` (`refactor: extract app dialogs props hook`)。
- 本刀内容：新增 `src/hooks/useAppDialogsProps.ts`，把 `AppDialogs` 那组 environment/settings/rerun/release/config/quick-create dialog 的 props 装配从 `App.tsx` 抽出；`App.tsx` 改为只传 `...appDialogsProps`。
- 为配合该抽离，`src/components/layout/AppDialogs.tsx` 的 props 接口已导出为 `AppDialogsProps`，便于 hook 复用。
- 验证通过：`pnpm typecheck`、`pnpm test`、`cargo check`（Rust 校验仍需在 `src-tauri/` 目录执行）。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P18`，下一步优先评估 history filter/preset 或 release checklist 相关剩余接线还能否继续收口。
- 已继续推进 `one-publish` 的 `PR-E`：提交 `a0b515b` (`refactor: extract preset text hook`)。
- 本刀内容：新增 `src/hooks/usePresetText.ts`，把预设配置名称/描述的本地化映射与 `getPresetText` 从 `App.tsx` 抽出；`App.tsx` 改为只消费 hook 返回值。
- 验证通过：`pnpm typecheck`、`pnpm test`、`cargo check`（Rust 校验仍需在 `src-tauri/` 目录执行）。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P19`。
- 已继续推进 `one-publish` 的 `PR-E`：提交 `4e13f1d` (`refactor: extract history view state hook`)。
- 本刀内容：新增 `src/hooks/useHistoryViewState.ts`，把 history filter preset / daily triage / filtered records 的派生与页面接线从 `App.tsx` 抽出；`App.tsx` 改为只消费 hook 返回值并透传给 `ExecutionHistoryCard`。
- 验证通过：`pnpm typecheck`、`pnpm test`、`cargo check`（Rust 校验仍需在 `src-tauri/` 目录执行）。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P20`，下一步优先评估 release checklist 或剩余 history/config 接线还能否继续收口。
- 已继续推进 `one-publish` 的 `PR-E`：提交 `b8b66f4` (`refactor: extract failure group selection hook`)。
- 本刀内容：新增 `src/hooks/useFailureGroupSelection.ts`，把 failure group 当前选中项的同步、缺省选择与 representative record 派生从 `App.tsx` 抽出；`App.tsx` 改为只消费 hook 返回值。
- 验证通过：`pnpm typecheck`、`pnpm test`、`cargo check`（Rust 校验仍需在 `src-tauri/` 目录执行）。
- `ACTIVE.md` 已自动切换到下一切片 `PR-E.P21`，下一步优先评估 release checklist 或剩余 history/config 接线还能否继续收口。
- 主人明确要求继续完成“执行系统落地版”，因此新增独立 simple activity `execution-system-spec-v1` 作为当前 focus，不再让运行真相继续指向 `one-publish-refactor`。
- 已起草三份落地文件：`docs/execution-system-spec-v1.md`、`contracts/one-publish-contract.md`、`references/templates.md`；定位为先落文档规范与模板，不立即强推第二轮实体迁移。
