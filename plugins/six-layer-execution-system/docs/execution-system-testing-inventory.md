# execution system testing inventory

Authoritative source-checkout inventory now lives at:

- `<repo-root>/tests/execution-system-testing-inventory.md`

This plugin-local file remains as a compatibility pointer because:

- standalone plugin copies do not ship repo-root `tests/`
- older roadmap/tasks/docs references still point at `docs/execution-system-testing-inventory.md`
- the testing inventory itself is now owned by source-checkout development assets, not distributed runtime assets

When you are working inside the source checkout, read and update the repo-root file first.
