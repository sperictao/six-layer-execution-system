# 2026-04-02

- 已将 extracted execution-system 的默认运行根收口到插件仓库根目录，`scripts/inspect_execution_system.py` 与 `scripts/run_execution_checks.py` 在默认环境下可直接工作。
- 已将 live ledger、checker 与文档入口统一改到插件根路径，`ACTIVE.md` 中 roadmap activity 的 `path`、`source_doc`、`roadmap_doc`、`tasks_doc` 与 closeout/handoff 路径均指向当前仓库根。
- 已建立插件根顶层 git 基线，强 git 校验继续以当前仓库为唯一落点，不再依赖旧 `workspace/` 或 `old_version/` 仓库状态。
- 已清理误迁入的外部业务文件与旧 prompt 资产，只保留 execution-system 本体所需的 contract、roadmap、tasks、docs、memory、scripts 与插件元数据目录。
- 已将历史顶层控制文件收口为 execution-system 专用 prompt/runtime 规则，并删除不再属于本体的控制文件。
- 当前 recovery 约定：运行真相只看 `ACTIVE.md`；`memory/2026-04-02.md` 仅记录此次 extraction / root consolidation / prompt cleanup / checker 回归结论，不承载 live state。
- 当前默认 review focus 为 `waiting-ledger-review`；`execution-system-spec-v1` 处于 maintenance posture，`execution-system-decomposition-upgrade` 保留可恢复状态但不继续自动扩展。
- 当前回归状态：
  - `python3 scripts/run_execution_checks.py checks --timeout 120` 通过
  - `python3 scripts/run_execution_checks.py full-tests --timeout 120` 通过
