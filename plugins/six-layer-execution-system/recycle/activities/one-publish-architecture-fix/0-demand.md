# one-publish 架构修复 demand intake

- request: 使用六层执行系统，先后修复 one-publish 项目的 P0、P1、P2 全部架构问题
- task_type: `refactor`
- desired_outcome: 将 one-publish 从「腰部良好、头尾臃肿」的沙漏架构改造为前后一致、可扩展、可测试的六边形架构，已识别的 10 个问题全部修复
- scope:
  - 仓库根 /Users/erictao/source/repos/one-publish
  - 前端 src/ (React 18 + TypeScript ~193 files ~7600 LOC)
  - 后端 src-tauri/src/ (Rust ~50 files ~8600 LOC)
  - 不改变任何外部 API 或用户可见行为
- constraints:
  - 所有改动必须是「低风险纯重构」：不改行为，只改结构
  - 每个 slice 完成后必须通过 pnpm typecheck（前端）或 cargo check + cargo clippy（后端）
  - 不引入新的运行时依赖，除非有充分理由（如 Zustand 替换手动 DI）
  - 遵守 one-publish 仓库的 AGENTS.md / Trellis 规范
- non_goals:
  - 不添加新功能
  - 不改变 UI 外观
  - 不修改 Provider trait 接口语义
  - 不迁移持久化格式
- risk_level: `medium`
- external_confirmation_required: `false`
- dependency_graph:
  - P0（架构债务）先于 P1（代码健康）先于 P2（可扩展性）
  - P0 内部：移除 execute_publish → 消除 PublishConfig dotnet 假设 → 提取 App.tsx 状态编排（此三项有依赖顺序）
  - P1 内部：各 slice 之间无硬依赖，可按 wave 并行
  - P2 内部：Command 中间件先于 Provider 动态发现（后者可能依赖中间件的 infrastructure）
- parallelizable_units:
  - P1 四个 slice 相互独立，可同波并行
  - P2 三个 slice 中，Provider 动态发现和事件总线可同波并行
- serial_units:
  - P0-1 移除废弃 execute_publish 路径
  - P0-2 消除 PublishConfig dotnet 假设
  - P0-3 提取 App.tsx 状态编排
  - P1 批次（4 个 slice 并行）
  - P2-1 Tauri Command 中间件
  - P2 批次（Provider 动态发现 + 事件总线 并行）
- expected_artifacts:
  - 9-10 个独立可回滚的 git commit
  - 更新后的 PROJECT_STATUS.md 或等效状态文档
  - 每个 slice 有对应的验证记录
- validation_plan:
  - 每个 slice 运行 pnpm typecheck（前端）+ cargo check && cargo clippy（后端）
  - P0 完成后运行全量 cargo test + pnpm vitest run
  - P1 完成后运行全量测试 + 手动冒烟（启动应用、扫描项目、执行发布）
  - P2 完成后运行全量测试 + 冒烟 + 新增 Provider 注册流程验证
- closeout_condition:
  - 所有 10 个问题已修复并验证
  - 全量测试通过
  - ACTIVE.md 标记 done
